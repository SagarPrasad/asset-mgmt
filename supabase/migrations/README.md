# Supabase Database Migrations Directory

From now on, all database schema modifications, new tables, and column alterations will be saved here as sequential `.sql` migration files.

The base complete schema is maintained at:
- `supabase/schema.sql` (Full clean recreated schema)
- `supabase/drop_all_tables.sql` (Clean reset script)
